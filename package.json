{
  "name": "mappo",
  "version": "1.0.0",
  "description": "mappo",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run test && ./node_modules/.bin/electron .",
    "test": "time node src/createDataReader.test.js && time node src/createVerge1MapLoader.test.js && time node src/createVerge1VspLoader.test.js && time node src/createVerge1VspConverter.test.js && time node src/createVerge1PalLoader.test.js && time node src/createVerge1ChrLoader.test.js && time node src/createVerge1ChrConverter.test.js && time node src/createTileGridConverter.test.js && time node src/createVerge1SmallFntLoader.test.js && time node src/createVerge1MainFntLoader.test.js && time node src/createVerge1BoxRawLoader.test.js && time node src/createVerge1SmallFntConverter.test.js && time node src/createVerge1MainFntConverter.test.js && time node src/createVerge1BoxRawConverter.test.js && time node src/createVerge1SpeechSpcLoader.test.js && time node src/createVerge1SpeechSpcConverter.test.js && time node src/createPcxLoader.test.js && time node src/createPcxConverter.test.js"
  },
  "keywords": [
    "mappo"
  ],
  "author": "Charles Rector <choosetheforce@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.3.4",
    "jquery": "^3.1.0",
    "lodash": "^4.15.0",
    "pngjs": "^3.0.0"
  },
  "devDependencies": {
    "expect": "^1.20.2"
  },
  "//": "node 5.10.0 is required for the new Buffer API (Buffer.from, etc.)",
  "engines": {
    "node": ">=5.10.0"
  },
  "engineStrict": true
}
