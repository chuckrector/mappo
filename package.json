{
  "name": "mappo",
  "version": "0.13.0",
  "description": "mappo",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run test && ./node_modules/.bin/electron .",
    "test": "time node runtests.js && time ./runtests-cli.sh"
  },
  "keywords": [
    "mappo"
  ],
  "author": "Charles Rector <choosetheforce@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.3.4",
    "jquery": "^3.1.0",
    "lodash": "^4.15.0",
    "pngjs": "^3.0.0"
  },
  "devDependencies": {
    "expect": "^1.20.2"
  },
  "//": "node 5.10.0 is required for the new Buffer API (Buffer.from, etc.)",
  "engines": {
    "node": ">=5.10.0"
  },
  "engineStrict": true
}
